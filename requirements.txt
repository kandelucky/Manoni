# Manoni dependencies — intentionally tiny (runs on a weak laptop)
# tkinter is part of the Python standard library (no install needed)
pillow
tkinterdnd2
tintkit>=0.2.2      # Dot + TitledSlider/Foldout `dot=`: the slider cost badges
