"""UI feature mixins for the Manoni window.

Each module here holds one slice of the window's behaviour as a mixin class.
`manoni.py`'s `Manoni` inherits from them all, so the methods keep sharing
`self` exactly as before — the code is just split by topic across files.
"""
